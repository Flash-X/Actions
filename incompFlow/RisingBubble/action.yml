# Name of the composite action
name: RisingBubble

inputs:
  github_token:
    description: 'GitHub credentials'
    required: true

#  amrex_image: 
#    description: 'amrex input image'
#    required: true

runs:
  using: "composite"
  steps:
    - run: pip3 install click
      shell: bash
    - run: git clone https://${{inputs.github_token}}:x-oauth-basic@github.com/Flash-X/Flash-X.git
      shell: bash
    - run: cd Flash-X && pwd
      shell: bash
#    - run: python3 container/incompFlow/RisingBubble.py ${{inputs.amrex_image}}
#      shell: bash
